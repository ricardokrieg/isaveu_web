<section class="">
  <div class="bg-image-holder bredcrumb bg-primary">
    <div class="container">
      <div class="row">
        <div class="col-md-8">
          <h2>Admin</h2>
          <hr class="hr_narrow  hr_color">
        </div>
      </div>
    </div>
  </div>
</section>

<div class="grid-wrapper section">
  <div class="container">
    <div class="row">
      <div class="col-12 col-lg-12 element-content">
        <div id="tables" class="element_single">
          <div class="display-single_element">
            <h3 style="margin: 30px 0">Detalhes <%= @budget.key.name %></h3>

            <a class="btn btn-primary btn-default" href="<%= url("/admin/#{@budget.key.name}/editar") %>">Editar</a>

            <form action="<%= url("/admin/#{@budget.key.name}/gerar-orcamento") %>" method="POST">
              <button type="submit" class="btn btn-success">Gerar Orçamento</button>
            </form>

            <br>
            <br>
            <br>

            <dl>
              <dt>Token</dt>
              <dd><%= @budget.key.name %></dd>

              <dt>Status</dt>
              <dl><%= @budget['Status'] %></dl>

              <dt>Criado Em</dt>
              <dd><%= @budget['Timestamp'] %></dd>

              <dt>Tipo de Serviço</dt>
              <dd><%= @budget['Tipo de Serviço'] %></dd>

              <dt>Nome</dt>
              <dd><%= @budget['Nome'] %></dd>

              <dt>Telefone</dt>
              <dd><%= @budget['Telefone'] %></dd>

              <dt>Whatsapp</dt>
              <dd><%= @budget['Whatsapp'] %></dd>

              <dt>Email</dt>
              <dd><%= @budget['Email'] %></dd>

              <dt>Comentário</dt>
              <dd><%= @budget['Comentário'] %></dd>
            </dl>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

